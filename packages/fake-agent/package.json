{
  "name": "@agenttown/fake-agent",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "company": "tsx src/company-cli.ts",
    "pretypecheck": "pnpm --filter @agenttown/probe-contract --filter @agenttown/runtime-contract build",
    "test": "vitest run",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@agenttown/probe-contract": "workspace:*",
    "@agenttown/runtime-contract": "workspace:*"
  }
}
