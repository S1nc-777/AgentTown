{
  "name": "@agenttown/probe-runner",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "probe:fake": "vitest run test/benchmark.test.ts && pnpm --filter @agenttown/fake-agent test",
    "score-frameworks": "tsx src/score-frameworks.ts"
  },
  "dependencies": {
    "@agenttown/probe-contract": "workspace:*",
    "node-pty": "^1.1.0"
  }
}
