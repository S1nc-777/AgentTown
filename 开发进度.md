# AgentTown 开发进度

> 本文档由"与 Codex 的开发对话（2026-07-18 ~ 2026-08-15，主线程 + 61 个子代理会话）+ 仓库 git 历史"整理而成，记录项目从概念到当前状态的完整开发历程、验证证据与剩余工作。

---

## 一、当前状态速览（2026-08-17 更新）

| 项目 | 状态 |
| --- | --- |
| 开发分支 | `codex/p1b-git-collaboration`（worktree：`.worktrees/p1b-git-collaboration`，本地领先远端 15 个提交） |
| 当前 HEAD | worktree `8bdfca3`（方向 A Task 6 验收完成） |
| 阶段进度 | P1A 100% · P1B 12/12 · 方向 A：Task 1-6 ✅（真实领导协作闭环跑通）、Task 7 问卷待内测、Task 8 决策初稿已写 |
| 最新验证 | Task 6 验收：**真实 OpenCode 领导自主 propose/assign + Git 协作闭环，双任务完成**；对照：单 Agent 45s/$0.0002/真实代码 vs 协作 ~10min/$0.05/机制闭环 |

**一句话状态（2026-08-17）：** "**方向 A 核心实验达成**：真实 Agent 领导（OpenCode）已能自主拆任务、派工并驱动完整 Git 协作闭环（提交→审核→集成→完成）。但对照实验显示：协作的速度/成本天然劣势，代码产出优势未验证（fake 员工只提交占位文件）——**产品价值主张需从'更快产出'转向'可监督的确定性协作流程'**，最终决策待问卷回收（见 `docs/requirement-experiment/decision.md`）。"

**2026-08-17 实验关键事实（对话实录）：**
- Task 6 验收全链路（详见 worktree `artifacts/requirement-experiment/run-2026-08-17/observations.md`）：真实领导 propose→task.created ×2 → assign → fake 员工 git 提交 → 审核 approved → 集成 → task.completed ×2，~10min、$0.05。
- 验收暴露并修复 13 项真实接入问题：npm shim spawn、claude `--cd`、opencode stdin 阻塞、ACTION 格式（类型枚举/taskId/assignee/dependencies）、**leader 驱动循环（架构缺口，新增功能）**、git 场景分配等。
- ⚠️ 2 个产品缺陷待修：CLI `core-process.ts` 硬编码 `AGENTTOWN_FORBID_REAL_PROBES: "1"`（真实用户无法 CLI 启动）；lease TTL 5s 自动暂停。
- 对照实验：单 Agent（opencode）45s/$0.0002 产出真实 todo 应用（7 测试通过）；协作产出 fixture 占位文件——**不同质，P1C 接真实员工后需重新对照**。

---

## 二、开发时间线

### 2026-07-18 · 概念与需求澄清（主线程）
- 用户提出完整产品设想（"把 agent 整合成一个公司"、调度器、看板、模板）。
- Codex 一次性提出 360 个产品问题；用户逐题回答，确立核心决策（详见《产品概念书》）。
- 确定"领导 Agent + 调度器"双层架构、固定员工名单、方案 B（混合控制平面）。

### 2026-07-20 · 立项与仓库建立
- 确定方案 B、AGPL-3.0 许可证、命名 **AgentTown**。
- 用户提供 GitHub 信息（S1nc-777 / AgentTown / 公开），仓库建立，首个提交 `d4f5c5c`、`6f6dbcc`（产品设计文档）、`d016ad9`（实施路线图）。
- 开始 **P0 可行性验证**。

### 2026-07-20 ~ 07-23 · P0 可行性阶段（提交 `eecc519` ~ `55c766d`）
- 建立 pnpm monorepo（packages：probe-contract / probe-runner / fake-agent / electron-spike 等）。
- 探针协议、确定性 Fake Agent、Windows PTY 探针、Codex/Claude CLI 能力探针、评分器。
- Electron 与 Tauri 桌面运行时评估（`spike: evaluate Electron runtime` / `spike: record blocked Tauri evaluation`）。
- **结论：没有合格桌面运行时，P0 判定桌面框架不可行/阻塞，转为先做 Headless MVP**（ADR `0001-desktop-and-core-runtime.md`）。
- 验证：189 项测试通过、3 项跳过；类型检查与 Fake Agent 探针通过。
- 分支 `feature/p0-feasibility`（未合并 main，保留实验结论）。

### 2026-07-26 · 路线修正（对话）
- 用户问"目前这个项目能用了吗"→ 明确"不能作为产品使用，只是实验基础"。
- 用户选择路线 A：**桌面 UI 移出关键路径，先做 CLI/后台内核**。
- 一次性确认 60 个 P1 设计问题；首版真实 Agent 定为 **Claude Code / OpenCode / Hermes**（用户指定，异于推荐）；采用能力驱动适配器方案（方案 1）。
- 借鉴华为、字节、海尔、腾讯、阿里、Amazon/Netflix 的运行机制，确立"公司宪法"七条制度。
- 确认"退出终端/UI 即暂停公司"（用户修正）、可恢复关闭、通用组织配置模型。

### 2026-07-27 ~ 07-29 · P1A：Headless MVP（提交 `d7d0c14` ~ `d2e831c`）
- `codex/p1-headless-mvp-design` 分支产出设计文档（`db5cafc` / `d7d0c14`）。
- `codex/p1a-core-fake-company` 分支实现：
  - 固定四员工 Fake Agent 公司（领导/开发/测试/审核）、任务 DAG、派发与审核；
  - SQLite 事实库 + append-only 事件流、Windows Named Pipe IPC、CLI（init/start/status/tasks/timeline/pause/resume/stop）；
  - 生命周期强化：进程归属、检查点暂停恢复、防残留进程清理（提交 7de361e~41139b1）。
- 子代理：p1a_task1~3（impl+review 成对）、p1a_final_review。
- **P1A 完成并入 `main`（`d2e831c`）**，含 release hardening 三轮审查修复。

### 2026-07-29 ~ 07-30 · P1B Task 1–9：Git 协作闭环（提交 `2ea80ab` ~ `94ec97d`）
- 07-29 一次性确认 100+ 项 P1B 规格（worktree 隔离模型、证据包、审核、确定性集成、冲突处理）。
- Task 1–4（07-29~30）：Git 契约（`ed215ba`）→ Schema v2 版本化存储（`3a0c666`）→ 仓库安全预检（`6ad8cf4`）→ 任务 worktree 生命周期（`45125ed`）。
- Task 5（07-30，提交 `dcc4de5`/`ff8bbba`/`baad570`）：权威验证命令执行，真实子进程证明无残留；经历 3 轮独立审查共修复 10+ 个 Critical/Important（路径 TOCTOU、进程树清理、跨公司越权、日志脱敏、事务原子性）。
- Task 6（`fb6f240`/`589c5ea`）：提交验证 + 不可变只读审核包（builder 防伪造、textconv 禁用、内容哈希）。
- Task 7（`e171e8b`/`632a13e`）：审核状态机 + Git 工作流协调器（fail-closed，杜绝绕过路径）。
- Task 8（`6668407`/`33d18d3`/`a10255b`/`b5dc9f1`）：确定性候选集成、CAS 原子推进 integration ref、跨 Git/SQLite 崩溃窗口对账、幂等 replay 门禁（4 轮审查，含 1 个 Critical）。
- Task 9（`946d1c5`/`0949128`/`94ec97d`）：Git 冲突 → 结构化冲突任务 → 精确重现冲突 → 解决提交走完整验证/审核/集成 → 原提交 `superseded` → resolution 再冲突持久化用户审批（3 轮审查）。
- 用户 07-30 指示"做完这个 task 之后停止"→ Task 9 完成后按要求暂停（08-12 恢复）。
- 每项验证数据：Core 测试 233 → 247 → 279 → 297 → 319 → 346 → 366 逐项增长，全部 0 失败；每项均通过独立 Spec/Code-quality 双审查。

### 2026-08-12 ~ 08-15 · P1B Task 10–11：重启对账与 CLI 接线（提交 `b54cf08` ~ `170f76a`）
- 08-12 用户问"现在做到了什么进度，还有什么没做"→ Codex 给出完整进度报告（9/12）→ 用户："继续完成 p1b"。
- Task 10（`b54cf08` + `24ba1f9` + `0c86f42`）：Git checkpoints、进程重启后正式对账、篡改停止、清理恢复；修复 CAS 后冲突解决恢复、old-SHA 安全回滚、真实暂停栅栏、审批幂等（1 Critical + 3 Important 修复）；最终 **Core 407/407**。
- Task 11（`170f76a`，未推送）：把 P1B 能力接入 IPC/CLI——工作区与证据查看、交付摘要、审批、先预览后执行的精确清理（默认只碰工作区）。
- 08-15 用户："继续，避免过于谨慎"→ Task 10 收尾、Task 11 推进；**Task 11 独立审查抓到 2 个不能忽略的清理风险（junction 目录越界删除、崩溃时删除顺序不可恢复）+ 4 个重要问题，正在修复（对话结束点）**。

### 未开始
- P1B Task 12：完整确定性 Git Fake Agent E2E。
- P1C：Claude Code / OpenCode / Hermes 真实适配器。
- P1D：真实异构 Agent 协作 Alpha 验收 + 演示录像。
- 桌面"赛博办公室" UI、安装包、模板 GUI 配置。

---

## 三、任务执行方式（对话中的工作流）

项目全程采用"书面计划 + TDD + 每项独立审查"的团队化开发（这也是项目自身理念的自举）：

1. **计划先行**：每个阶段先产出设计文档（`docs/superpowers/plans/*`、`docs/superpowers/specs/*`），用户确认规格后再实现。
2. **TDD**：每个任务先写测试（RED）→ 最小实现（GREEN）→ 完整回归。
3. **实现 + 审查分离**：每项任务由 `*_impl` 子代理实现、`*_review` 独立子代理审查（Spec + Code-quality 双维度），审查者不采信实现者报告，主控复验后才算完成。
4. **审查发现即修复**：多轮修复-复审循环（如 Task 8 经历 4 轮审查、Task 5 经历 3 轮），发现 Critical/Important 必须补回归测试。
5. **如实报告环境噪声**：Windows 真实 Git 夹具的 EPERM/EBUSY 清理竞态一律不改超时、不重试掩盖，如实写入报告。
6. **中断恢复**：恢复时先检查工作树与运行中测试，不重复执行、不并发 Git 测试。

子代理命名（对话中的"员工"）：Helmholtz（task5_codex）、Popper（task5_review）、Russell（task9_benchmark）、Franklin（final_branch_review）、Carver（p1b_task4_impl）、Ampere（p1b_task4_review）、Lorentz（p1b_task10_impl）、Euler（p1b_task9_review）等。

---

## 四、代码仓库结构（当前）

```
D:\AgentTown\
├── packages/
│   ├── core/            # 调度内核（SQLite、事件、任务、策略、Git 服务）
│   ├── cli/             # CLI 入口（init/start/status/tasks/timeline/pause/resume/stop/deliver/cleanup）
│   ├── fake-agent/      # 确定性 Fake Agent（测试与演示用）
│   ├── probe-runner/    # P0 可行性探针
│   ├── probe-contract/  # 探针协议契约
│   ├── runtime-contract # 运行时契约（v2）
│   ├── e2e/             # 端到端测试
│   └── electron-spike/  # Electron 可行性实验
├── docs/
│   ├── adr/             # 架构决策记录（0001：桌面与 Core 运行时）
│   ├── development/     # 开发状态与经验（status.md 等）
│   └── superpowers/     # 各阶段计划与规格
├── artifacts/           # 可行性实验产物（真实探针摘要等）
└── scripts/             # 辅助脚本（真实探针等）
```

---

## 五、验证证据汇总

| 里程碑 | 测试规模 | 审查 | 远端提交 |
| --- | --- | --- | --- |
| P0 可行性 | 189 通过 / 3 跳过 | final_branch_review 通过 | `55c766d`（feature/p0-feasibility） |
| P1A Fake Company | P1A 门禁 9/9 | p1a_final_review 通过 | `d2e831c`（main） |
| P1B Task 5 验证 | 247/247（含定向 32/32） | 双通过（3 轮修复） | `baad570` |
| P1B Task 6 审核包 | 297/297（含定向 50/50） | 双通过（2 轮修复） | `589c5ea` |
| P1B Task 7 工作流 | 319/319（17 文件） | 双通过（2 轮修复） | `632a13e` |
| P1B Task 8 集成 | 346/346（18 文件） | 双通过（4 轮修复） | `b5dc9f1` |
| P1B Task 9 冲突 | 366/366（19 文件） | 双通过（3 轮修复） | `94ec97d` |
| P1B Task 10 对账 | 407/407 | 双通过（修复 1C+3I） | `0c86f42` |
| P1B Task 11 CLI | CLI/渲染 18/18、IPC 77/77 | **审查发现 6 项问题，修复中** | `170f76a`（未推送） |

---

## 六、已知问题与剩余工作

### P1B 剩余
- **Task 11 收尾**：修复独立审查发现的清理风险（证据目录 junction 越界删除、"先删文件后写数据库"崩溃不可恢复）与 4 个重要问题，复审通过后推送 `170f76a`。
- **Task 12**：完整确定性 Git Fake Agent E2E，验证整条 Git 公司闭环（worktree → 提交 → 验证 → 审核包 → 审核 → 候选集成 → CAS 推进 → 任务完成）。

### P1B 之后
- **P1C**：Claude Code / OpenCode / Hermes 真实适配器（能力驱动，PTY 管理，不支持能力明确降级）。
- **P1D**：真实异构 Agent 协作完成一个项目的 Alpha 验收（默认组合：Claude 领导 + 2×OpenCode 开发 + Hermes 审核），含演示录像。
- **桌面"赛博办公室" UI**：工位、气泡、Token/上下文展示、点击接管、任务看板、系统托盘（UI 只能通过 Core IPC 操作，不得直接控制 Agent）。
- **发布准备**：Windows 安装包、一键初始化、Agent 自动探测、模板 GUI 配置、独立 LICENSE 文件、贡献指南、正式版本发布流程。
- **恢复缺口**：若程序恰好在 Git ref 已推进、SQLite 尚未提交的窗口崩溃，需确认 Task 10 对账机制覆盖完整（对话 08-12 记录的当时缺口，Task 10 已实现，待 E2E 验证）。

---

## 七、开发经验沉淀（对话与代码中反复出现的教训）

1. **桌面框架不能放在关键路径上**（P0 教训）：Electron 后台核心存活失败、Tauri 工具链阻塞 → 先做"能干活"的内核，UI 永远只是 Core 的客户端。
2. **确定性调度器是防幻觉的第一道防线**：Agent 可以申请和决策，但不能直接修改公司的客观状态；所有状态必须由 Core 从权威事实（Git 派生、数据库记录）重新验证，绝不信任 Agent 的文字报告。
3. **"不能靠 happy path 掩盖问题"**：每个原子存储 bundle 必须独立验证完整身份绑定（run/task/attempt/workspace/submission/事件 payload），直接调用入口也要防伪造。
4. **失败必须 fail-closed**：Git 任务工作区记录异常只能报错，不能掉回旧兼容流程；无可靠身份源时保守返回"不可验证"。
5. **崩溃窗口要"留下可判断但不擅自修复"的状态**：prepared/aborted/conflicted/committed 各状态必须有明确的幂等重放语义，绝不自动重试 Git 操作或猜测恢复。
6. **跨系统一致性（Git ↔ SQLite）是最大事故源**：integration ref、worktree、数据库事实三方必须原子推进，CAS 推进 + 事件绑定 + 对账。
7. **Windows 环境的诚实纪律**：真实 Git 夹具的 EPERM/EBUSY 清理噪声如实上报，不放宽超时、不加重试；进程树清理必须有绝对截止时间并检查孤儿进程。
8. **用户接管是产品承诺，也是安全边界**：接管即暂停自动任务、记录人工干预事件、向领导同步摘要。
9. **"实用优先然后做取舍"**：不模拟薪资、考勤、绩效等无实际作用的公司制度；不引入不必要的新依赖。
10. **避免过度谨慎但不放松安全**（08-15 用户指示"继续，避免过于谨慎"）：重复性低收益检查可跳过，但用户文件安全与交付信息真实性相关的审查缺口必须修复。

---

## 八、如何继续开发（给下一个会话的交接说明）

- 主仓库在 `D:\AgentTown`（已从旧路径 C:\Users\S1nc\Documents\Codex\2026-07-18\7-30-slack-plugin-slack-openai 迁移，worktree 已 repair）。
- 开发分支：`codex/p1b-git-collaboration`（worktree：`D:\AgentTown\.worktrees\p1b-git-collaboration`）；当前 HEAD 分支 `codex/p1b-git-design` 为设计分支。
- 继续任务：Task 11 修复（见第六节）→ Task 12 E2E。
- 验证命令（对话中常用）：`pnpm typecheck`、`pnpm test`（Fake 探针禁用真实 Agent 环境变量）、`pnpm probe:fake`、包级 vitest 定向测试。
- 完整对话档案：主线程转录 `agenttown-main-thread-transcript.md` + 61 个子代理会话摘要（`agenttown-sessions/`），位于用户桌面，可作上下文重建参考。
