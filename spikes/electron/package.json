{
  "name": "@agenttown/electron-spike",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "dist/main.cjs",
  "scripts": {
    "build": "node scripts/build.mjs",
    "package": "pnpm build && node scripts/package.mjs",
    "pretest": "pnpm build",
    "test": "vitest run",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "node-pty": "^1.1.0"
  },
  "devDependencies": {
    "@electron/packager": "20.0.3",
    "electron": "43.2.0",
    "esbuild": "0.28.1"
  }
}
